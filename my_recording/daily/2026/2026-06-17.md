

# 1 今日目标
编译问题
protobuf 冲突
quic-go 不兼容
把food-a'd's切换到


运行问题
`data-server` 启动 panic：根因是 同一份 proto（`o2oalgo_datamanagement`）被两个 Go 包路径各注册了一次：
### . `food-ads-common`（你改的）

把对旧 `o2oalgo_datamanagement` 的引用迁到 `foodads_dataserver`：

- `dao/o2oalgo/store_dao.go`
- `dao/foodalgo/model/search_store.go`
- `dao/foodalgo/model/search_store_mainsite.go`

### 2. `common-lib`（我改的）

删除 `common-lib/shark/data.go`。

- 该文件 import 了 `shopeefoodads/common-lib/protobuf/o2oalgo_datamanagement`，只要任何代码 import `shark` 包就会触发重复注册。
- 功能已由 `common-lib/shark/foodads_dataserver.go` 替代（`FoodAdsGetData` 等）。
- 删除后，用本地 common-lib 验证，data-server 依赖树里不再包含 shopeefoodads 版 `o2oalgo_datamanagement`。



# 2 关键问题

# 3 新insight

# 4 Bug / Root Cause

# 5 阅读

# 6 明日待办
