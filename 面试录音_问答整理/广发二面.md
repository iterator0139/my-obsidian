---
tags:
  - 面试录音
  - 转录
transcribed: 30-May-2026 10-31-12
source: 广发二面 (transcribed on 30-May-2026 10-31-12).txt
整理形式: 问题+回答
忠实度: 回答尽量保留原始转录，仅做问答切分和必要断句
---

> 整理备注：源转录没有说话人标签，且长段技术回答与追问连续粘连；以下问答边界可能需要回听确认。

## Q1. 自我介绍

**问题：**
麻烦你那边做一下自我介绍吧。

**回答：**
好的，面试官你好，然后我叫程鸿。然后我目前是在Shopee负责广告架构以及AI agent架构的一个开发。

然后在Shopee主要是这两个项目经验。首先第一个的话是作为一个核心开发者，然后参与了广告系统全链路的从0到1的搭建以及它的一个持续迭代。然后在这个项目里面我主要负责的内容包括工程架构的一个pipeline的搭建，然后其次还有深度参与了整个算法机制的一个策略的设计和开发。然后这段经验主要是让我对整个在新架构以及在线的这种机制策略算法的效果优化的一个流程是有比较深入的一个实践和理解。

然后第二个项目的话就是主导了这样一个商家运营智能助手agent这样一个落地。然后主要是去解决广告商家运营他在工作流程中的一个知识获取，然后数据查询，然后包括他的一个复杂的归因诊断和角色优化这三个环节的一个效率低下的问题。然后通过去设计一个多agent的这样一个混合多级的agent架构，然后包括以及四级的这样一个机机制去解决复杂任务的一个路由以及复杂任务它长程任务的一个状态管理的问题。

然后上一段工作经历主要是在百度做机学习平台的开发。然后这块的话主要是去做一些机学习框架的优化以及模型分析体制的建设。然后第二部分的话就是模型调研这样一个端到端的这样一个一站式平台，它去对接以模型仓库为中心去对接上游的那个模型训练，然后下游的一个模型上限，然后整体串联起来这样一个模型调研的全流程去做到一个端到端的一个效率提升。

整体的话我的一个技术领域主要是在在新AI架构，然后具备工程和算法的一个双视角，然后整体是这样的。

## Q2. 两个项目分别做多久

**问题：**
你做的主要就是运营还有推荐系统是吧？这两个项目分别做了多久？广告系统也是0到1构建吗？团队多少人？

**回答：**
对，在线广告系统和这个内部的商家运营智能助手系统这两个。

第一个在线广告的项目它其实持续还是在一直的跟进，相当于是现在有接近三到四年的这样一个情况。然后智能助手的话这个已经是一年半的时间。

对，广告系统是从0到1来打点完成的，然后我是全程参与在系统。我们广告团队的话当时是有10个人左右，然后去做了一个廣告和算法在一起的這樣一個敏捷小組。

## Q3. 广告项目主要负责什么

**问题：**
那你主要是负责什么？

**回答：**
我主要是負責工程的實現。然後整體來說是這樣的，就是有几个阶段。最开始的话我刚刚参与这个项目的时候更多的是负责整个工程连路的这样一个pipeline搭建，然后以及我们的数据流和我们在线引擎的这样一个工程上的开发。

然后后面的话我们就开始进入算法的一个迭代期，然后我就是主要去负责整个算法框架的搭建，包括我们的模型校准模块，然后我们的一个预算平滑模块，我们的智能出价模块，然后我们的混排模块。然后这一部分的话都是我作为一个核心开发者，然后去深度参与到这样一个算法的一个设计和讨论以及落地实现上。

## Q4. 召回、精排和模型机制设计

**问题：**
讲一下这些各模块的一些模型设计，召回、粗排这些。重点介绍设计开发中遇到的困难以及怎么解决。

**回答：**
好的，然后您刚说召回出牌的话，这些主要是它的一个pipeline的一些搜推广站推荐和广告这样一个通用的这些流程。然后我刚刚提到的那些主要是广告机制策略的，然后我就分开讲一下。

首先召回的话这一块我们其实是有搜索广告和推荐广告。然后的话搜索广告的话这块最开始最初的时候主要还是用那个bmr5的这样一个西数检索，然后文字解锁来直接去做一些关键词的一些匹配，包括它的一个bid world的召回，然后它的一个我们的一些store name这些一些关键词的一些各路召回的一个多路照回这样一个机制，然后再去做一个融合的一个排序。

然后除了这个bmr.n5的话，我们在推荐场景的话还会有相当的一个照回。它主要是通过这样一个用户兴趣的这样一个相似度，然后去做一个相当的一个照回。然后这块的话我们是有这样一个embedding这样一个宣言的一个方案，主要是去提升它的一个CTR的目标以及它的一个相关性的目标这样目标在它的一个embedding的训练里面。

然后它里面其实特征设计的话它其实包括store内幕的特征，然后我们query内幕的特征，然后还包括我们query和store这样一个交叉特征，然后去做这样一个embedding的一个相关性的召回。然后这是召回侧的两个召回方法。

然后的话然后再进入到我们的我们这里没有出牌，然后我们直接进入到我们的一个金牌。金牌的话我们现在的线上模型主要是包括CTR模型，然后CVR模型，然后有一个OrderPrice模型。然后CTR模型的特征迭代和CVR的特征迭代这一块我没有去深入的参与，然后具体的特征的设计这块我可能了解了解。

那我就讲一下我们那个PID的一个控制的一个机制吧。它相当于是一个广告的一个在线的一个实时电路做调控的这样一个算法机制。然后它整体的思路就是需要去有一个我们的一个目标的一个值，比如我们的一个adload这样一个调控的目标。然后我们需要去有一个目标的adload，然后我们有一个实时的adload，然后基于这样一个误差值，然后去做一个包括三个维度的像积分项，然后微分项，然后我们当前窗口的这样三个维度这样一个累积误差的一个设计。

然后在这个过程中它其实有一个比较困难的点，就是在算法策略上我们怎么去做这样一个它的一个参数的一个调整。就比如因为它每一项积分项，然后微分项和它的一个当前窗口项这样一个三个维度，它都有自己的一个超参的系数，然后去决定你当前这样一个调控系数于哪一个为主。

然后基于这样一个背景的话，然后我们是去理线去搭建了一套这样一个离线仿真的这样一个系统，然后去做了一个离线搜餐的这样一个反正环境，然后去做到一个离线搜餐的这样一个机制策略。然后这是一个比较大的一个难点。

## Q5. 推荐系统在线延迟

**问题：**
整体推进应该是实时的吧？延迟怎么考量？

**回答：**
对对对。具体来说的话我们的延迟首先它是几个部分。然后照回的话这块我们的延迟大概在100毫秒左右，然后我们的排序的话会延迟会高一点，大概要200多毫秒的样子，然后做一个多路的这样一个模型的并行的一个打分。

然后其他的机制策略这一块就整体来说它的一个耗时都还比较正常。对，然后我们整体的连路的耗时大概是在600毫秒的这样一个水位。然后我们做的一些性能优化的话其实包括一些像我们整个pipeline上面的一些批处理，然后我们的一些异步的一些处理，然后包括像我们还有一些缓存，然后这样一些通用的这些工程连路上的一些处理。

## Q6. 多 agent 项目整体架构

**问题：**
聊一下后面的多agent项目，整体架构是怎么设计的？

**回答：**
整体的架构是基于意图理解的分发的路由机制。下游的话会接入三个agent的能力，包括像IG的agent，然后Test2Circle的agent，然后第三个就是基于Planesq的这样一个复杂任务的agent。

然后意图理解的话就是会把用户的 query 意图去分类成五类意图，包括 unknown 意图，然后闲聊意图，然后是刚刚提到的知识查询意图，然后数据查询意图，然后复杂归因诊断的这五类意图，然后分别去对应的做这样一个任务的路由。然后这是一个整体的架构。

然后再展开来讲，每一个agent他的价格的话像第一个rig的agent，我们现在的架构是一个react范式的，相当于它会去自主的决定去何时搜，然后搜什么，然后是停。然后我们的一个眼镜的路线是最开始是做的这种simple IG，然后就是做单次检索以及单次生成。

但是我们在跌来的过程中就发现它这样一个单次检索的一个问题，就是你的分块机制导致你整个的上下文会有碎片化的问题，你的上下文不全，然后你单次检索经过你的照回排序截断之后，然后它的会存在一些上下文缺失的问题。

然后我们去做在SimpleIG这一版我们也做一些优化，包括像我们的这样一个上下文的拓展，就是我们基于两集的这样一个chunk的组织的一个形式，就是负子的chunk分块机制。就是我们会通过子chunk这样一个细粒度的chunk去做一个检索，然后检索回来之后，然后再去路由到我们的一个负chunk，这样去把它的一个上下纹去给它扩大，去解决的就是你的分块机制和你的真实的上下纹之间存在mismatch这样一个不匹配的问题，然后通过你补全你的上下纹去缓解这样一个上下文缺失的问题。

这是我们SympIG的时候做的一个优化。然后后来的话我们就去迭代到这样一个ReactIG，然后去进行一个React翻译控制。就是每一次query接收到query之后，我们会通过模型来思考，然后它要去调用一个REG的工具，然后它的query是什么也是通过模型来控制。然后每一次生成快乐之后我们再去进行一个单次的这样一个rig的检索，然后检索回来的结果模型再去看，看到检索回来的结果之后，然后再去进行再一次的思考，然后去决定它是否下次要继续做检索还是说已经可以生成答案。

现在是这样的一个架构。然后这种范式的话它也是能够很好的去解决这样一种多挑问题，因为它天然的和人类去获取信息的这样一个机制是一致的。像人类的话它也是会去多次检索，然后去反复评估这样一个过程。

## Q7. Test2Circle 架构

**问题：**
Test2Circle 这块怎么设计？

**回答：**
Test2Circle这块的话我们是也是经历了一个迭代。然后最开始的话我们一般就是直接通过把所有的表啊，然后我们所有的列，然后以及我们用户的query，然后去做一个大的prompt去让模型一次生成。但是我们发现这样一个circ生成的一个准确性是非常的差的，然后包括会有很多的一个幻觉。

所以我们就进行了这样一个架构的一个拆分，就把circ生成的这个任务把它拆成了一个多阶段的一个流水线形式。它就包括我们的这样一个 query 的理解和改写，然后第二步是我们的一个 schema 的 linking，包括我们的表的照回和列的照回。然后第三部分的话就是会通过这样一个 column exploration 的这样一个机制去解决用户的 query 里面提供的查询条件和我们数据库里面 DB 真实的 value 它不配的这样一个问题。

然后最后的话就是通过 Circle生成阶段采用这种共识的一个并发生成的机制，然后去换取它的一个结果的一个稳定性和准确性。然后同时也采用这样一个反思机制去让模型能够去进行有结果反馈的这样一个从事，然后这是Test to Circle的部分。

## Q8. Plan execute 复杂任务 agent

**问题：**
复杂任务归因的 agent 是怎么做的？

**回答：**
最后的话就是基于Plan execute这样一个复杂任务归因的这样一个agent。然后它主要是去解决我们广告场景下广告运营去定位广告主投放过程中的一些投放问题，包括像这些爆光下降为什么爆光会下降，它的 ROI 为什么会下降，然后它的一个 CBC 的一个成本为什么会上升，这些广告投放期间的一些效果问题。

然后这个的话我们一开始也是通过 Dynation 来做。但是呢单agent的话，因为这是一个多步骤的这样一个过程，然后你然后同时也包括多个职责，包括你的一个数据拉曲，然后你的一个冲突分析，然后你的一个归约最后的一个逻辑判断分析，它是相当于是一个多职责的一个过程。然后我们把它放在一个agent来做的时候就发现它的一个上下文会持续的一个膨胀，然后会对它的一个任务的可用性会有比较大的一个负面作用。

然后后来我们就是把它给拆成一个planned SQL的这样一个机制。然后去通过这样一个职责节偶的这种方式去能够同时能够做到第一个是上下文的一个隔离。因为我们的planned agent和subagent它的一个上下文是完全隔离的，各自有各自的上下文。他们只是通过在任务尾派的时候主agent去把它的一个任务以及它的把subagent的一个任务以及他的一个角色，包括他的一些输入输出和工具这些信息可以通过这样一个prompt方式去给他传递给subagent。其他的中间任务执行的三小文他们是互相隔离的，所以的话这种方式就能够解决三小文膨胀带来的注意力丢失的问题。

然后第二个的话就是plan and execute这种机制的话，先引入plan的话我们可以在plan这一部分去能够获得更好的一个执行路径的可控性，不会像react那种它会去对于同一个任务它会有多次多次，同时的话它每一次的路径它都是不一样的。

然后我们这里plan的一个机制的实现就是通过我们的这样一个DAG图的一个定义，然后去做一个 DH7图的一个让Player模型去生成一个我们能力范围内的这样一个DH7图。然后再去通过DH7图去做一个Graph的一个一个构造，然后Graph构造我们去通过调度引擎来去生成我们的一个subtask。subtask之间的并行调度就是按照我们的Graph的一个定义来做一个执行。然后最后等到我们接连执行完成之后再把我们接着SAMURAI返回给我们的主Agent，主Agent再根据他的一个返回结果来进行下一步的执行动作。然后整体的价格就是这样。

## Q9. 多轮 RAG 上下文不一致和噪声

**问题：**
多轮的IG这里，会不会出现上下文不一致，检索到相悖内容导致答案混乱？

**回答：**
这里的话其实是一个质量保证的问题。就是因为我们的其实您刚说的情况主要还是因为他的一个query的一个两个维度，一个是它query的质量，第二个就是一个它的IG的质量。然后所以我们也会通过这两个维度来去优化这样一个问题。

首先query的质量这里的话我们在做一个训练的时候，它其实是会有一个对于query召回的结果和你最终答案结果的这样一个打分的，就是会去鼓励它生成更有效的query。更有效的query怎么判断，就是你照回的结果和你的最终的结果是不是匹配程度的一个相关性，就相当于我希望我这个query能够照回更多有效的信息，然后我们就认为这个query是一个好的query。

然后第二个的话就是我们去提高我们这样一个rig检索的一个准确性，然后我们会通过提高它的一个通过多路照回，然后再通过我们的一个排序，然后去做这样一个Trump的这样一个优选。然后去尽量让它能够生成照回出我们这样一个更加相关的这些Trump文道，去避免有这些噪声的一个传入。

然后如果有了噪声的话这块我们现在还没有去做一些更好的这些防护处理。但是我理解有了噪声确实它会对整个的一个结果生成会有一定的影响。所以我们可能更需要去加一道这样一个判定和准入的这样一个机制，就是对于我们的结果的准入，我们需要有一套管控的机制。不管是你通过你的相关性的一个预值来做一个管控，还是说通过大模型的这样一个泛化能力去做这种，要么加起来做管控，我们可以在这一部分去加一个这样一个准入的机制，然后去缓解它这样一个造生注入的问题。

## Q10. Schema Linking 怎么把表降到 3-5 张

**问题：**
test to circle 是 workflow 形式吗？怎么把50张表降至3-5张表，核心在哪里？

**回答：**
对它是一个工作流的形式。

它主要是通过Schema Linking。Schema Linking其实就是跟我们的照回排序是一样的机制。然后我们会把我们的表都构建成这种语意结构化的这样一个描述。然后对每张表我们会有三层的这样一个语意构建，包括它的一个业务背景，它的一个生场景，然后它的一个每个业务字段这样一个说明。然后这部分把它去做一个打包的构建，然后去存我们的这样一个相当索引库。

然后我们就去通过这样一个表的检索以及列的检索这样一个过程，去召回跟我们query更相关的这些表。然后再去召回完之后，我们还会去通过大模型来过一次它的一个精选的这样一个机制。就相当于是我们照回虽然是通过embedding来照回，但是embedding的话它往往有的时候只是语意上就是文本上的一个相关性，但是它可能我们再通过LIM来做一次真实语意上的一个相关性的过滤。然后所以通过这种照回排序结端的这种机制去降低我们这样一个候选的一个数量。

## Q11. 多 agent 框架参考

**问题：**
多Agent市面上有很多框架，有去参考吗？

**回答：**
多A-Z的话我们主要还是看的那个Clark Code的一些设计理念。像Clark Code的话它就是一个单agent这种react这样一个机制。然后它主要是也是通过接入很多的这样一个工具，然后我们的一个MCP，然后它去做一个整个agent执行路径的这样一个Harness的工程，然后去保证它整个长程任务的这样一个实行的稳定性，包括它的一些数据的终端，然后它的一个to do list这样一个状态的维护。

然后这是我们去接近它的一部分，就是包括像我们这个文件系统记忆，然后我们的工作区记忆，然后去其实这里面就是吸收了它的一些思想。

然后像其他的一些多agent的这样一个框架我还了解到包括像有这种autogen这样一个，它是一个更加偏向于去解决怎么快速创建这种多agent协作之间的这样一个工作的范式的一个框架。然后它的主要的一个设计点主要的一个内容的话就是它已经给你制定好了已经给你设计好了多个agent之间是怎么进行任务的派发，怎么做这样一个通信。然后它怎么通过这些基础的他们那些底层的API能够去快速创建出一个agent team的这样一个概念。

它其实内部也内置的很多这种多一整一个工作范式，包括像你的一个hands off的一个工作模式，然后你的一个swarm工作模式，然后plant is killed的工作模式。它其实这一部分的话它是有一个比较完善的这样一个开箱即用的一些机制，但它整体的话就是会比较重，然后上手的难度也会相对高一点。

所以我们当时的话第二的话我们还去调研过这个longchain。它其实因为我们workflow的test2circle就是用的longchain，但是我们在这里planet-sql多agent的时候，我们发现其实longchain的话，因为它的一个长程任务的这样一个状态维护，然后以及上下文的这些管理，它其实做的比较欠缺。它更多的是能够支持一个workflow的这样一个快速的搭建，以及它的每一个state这样一个持久化以及持久化和中断这些workflow更加对workflow有好的这些特性。

所以的话我们就是选择了去自己来做这样一个任务的这样一个持续化的这样一些长程任务的这样一些机制，然后自己去保证它的任务执行的稳定性和准确性。

## Q12. 任务失败、重复执行和 replan

**问题：**
任务执行时会不会出现任务失败或者重复执行，跟一开始plan不一致，怎么解决？

**回答：**
对这里也是有一些失败的问题是要去做一些设计的，有几个纬度吧。就是首先第一个是我们的一個 Plan維度。Plan維度的話其實這部分也會存在一些失敗的問題，就包括plan的一個結構化的結構的生成失敗，然後它的一个语意的一个创建式版。

这个话我们是通过了一个两层的一个教验来做。第一个的话就是去做一个结构性教验，包括它是会有环，然后它有没有孤立的节点，然后再包括它的一个整个的一个定义的格式是不正确的，这是结构化维度的。第二个的话就是它的一个语意维度，就比如它会不会生成我们系统其实没有的能力。比如我们已经在上下文里面给他提供了一些我们已有的这些工具和我们一些agent，然后但其实他在生成的时候又创建了一个subtask，但其实subtask我们的系统并不支持。然后这部分的话我们也是通过AM来做一个教验。

然后第二层的话就是您说的任务执行部分。任务执行部分的话我们也是有某一个我们把任务执行的部分的话我们分成了每一个节点，然后它会有两种失败。一种的话就是偶发性的失败，偶发性的失败就是包括你的一个能够超时，然后或者说是三出错误，这种话我们就是通过这种有限次的一个重视来去解决这些偶发的这不稳定问题。

然后有一种就是已经和我们当前的目标已经偏离了，我们的系统已经无法再任务无法再推进下去了。这种耗用其实会引入一个在关键节点，我们会引入一个这样一个大模型来做一个verifier的这样一个过程，然后去让他评估当前和我们这样目标的这样一个差距。然后如果是已经有已经完全已经评估出来完全是和我们目标差距不大或者有冲突有矛盾的时候，这个时候我们就会去做一个失败的标记以及失败的传播。我们就会把当前节点和他所有下一个节点标记为失败，然后失败之后我们这一次就整个plan就停止执行，然后再把这个结果返回给我们的plan模型，让他去做一个局部的replan。然后整体的一个失败的机制是这样的。

## Q13. 是否做过大模型训练和推理

**问题：**
你项目主要做一些黑水工程上的事情，对于大模型的一些方向有做过吗？

**回答：**
大模型具体的微调和推理这一块没有真实的工业级的经验，但是整个的模型训练，然后推理的机制原理我是了解的。

我这边没什么问题。这边如果问完了的话，那我们这边面试就先到这里结束了。然后感谢您参加我们的一个面试，麻烦您这边先退后一下，您的面试会稍微留一下事情。好的。
