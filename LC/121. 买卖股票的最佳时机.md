贪心法

维护前序的最小值，计算当天卖出和最小值之间的差值，和最大利润进行比较

完整dp的方法是定义每天的持有股票和不持有股票的最大收益
$dp[i][0] = max(dp[i-1][0], dp[i-1][1]+prices[i])$
$dp[i][1] = max(dp[i-1][1], dp[i-1][0]-prices[i])$

如果限制了只能交易一次
$dp[i][0] = max(dp[i-1][0], dp[i-1][1]+prices[i])$
$dp[i][1] = \max(dp[i-1][1], -prices[i])$ 因为买入前利润必须是0
