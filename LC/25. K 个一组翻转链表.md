给你链表的头节点 `head` ，每 `k` 个节点一组进行翻转，请你返回修改后的链表。

`k` 是一个正整数，它的值小于或等于链表的长度。如果节点总数不是 `k` 的整数倍，那么请将最后剩余的节点保持原有顺序。

你不能只是单纯的改变节点内部的值，而是需要实际进行节点交换。

递归思路：
如果还有K个，反转这K个，然后递归处理下一组的头结点，再把反转后的K个和递归处理反正成功的链表连接。
递归的定义：K个一组反转链表，返回反转后的头结点
递归出口：如果不足K个，直接返回原头结点
递归处理：对前K个进行标准反转，然后把反转后的头结点连接到递归处理后的头结点

```
class ListNode:
	def __init__(self, val=0, next=None)
		self.val = val
		self.next = next

func inverseListKGroup(head)
	

```


迭代思路：
从start节点开始，K个一组反转，然后连接前一组的尾结点和反转后的头结点，再更新新的start节点和前一组的尾节点