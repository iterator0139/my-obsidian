  我建议从第一性原理把研发拆成四个互相正交的问题：

   问题    要回答什么                                  不应混入什么
  ━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━
   语义    系统必须产生什么结果、绝不能产生什么结果    类、函数、框架实现
  ──────  ──────────────────────────────────────────  ────────────────────────
   架构    哪个边界拥有事实与决策，组件如何协作        测试 fixture、局部算法
  ──────  ──────────────────────────────────────────  ────────────────────────
   实现    在某个模块中怎样兑现分配给它的责任          重定义系统业务规则
  ──────  ──────────────────────────────────────────  ────────────────────────
   证据    如何证明语义与设计真的成立                  把测试细节写成行为本身

  同时还有一个独立的“范围”维度：系统、能力、模块、一次代码变更。每份产物都必须同时标注“属于哪一层问
  题”和“覆盖哪个范围”。

  核心关系应是：

  系统 / 能力语义
      ↓ 分配责任
  总体架构
      ↓ 分配局部契约
  模块设计与实现
      ↓ 提供局部证据
  集成与能力验收
      ↓
  证明原始语义

  这不是纯瀑布：每一层发现无法兑现时，都可以回到上层修订；但不能由下层代码悄悄改写上层语义。

  一个完整的 AI Coding 流程，我会这样设计。

  ## 0. 问题定界与基线核验

  先确认：

  - 用户要解决的真实问题与成功结果；
  - 当前系统实际行为；
  - 已有约束、兼容性和明确非目标；
  - 这是一个新能力、重构、缺陷修复，还是局部优化。

  产物不是设计，而是一页“问题框架”。它防止模型一上来就把现有代码误当成目标。

  ## 1. 能力语义契约

  这是最高层、最稳定的产物。以一个用户可感知的能力为单位，而不是以模块为单位。

  内容只有：

  - 触发条件和可观察结果；
  - 业务状态与合法转换；
  - 系统级安全规则；
  - 非目标和失败语义；
  - 外部可见的兼容性约束。

  例如取消能力的系统语义是：取消被运行者受理后，不能继续产生新的业务动作；cancelling 不等于
  cancelled；不能把未确认停止伪装为完成。

  系统级不变量只放这里。它们通常是：

  - 业务安全：不会错误扣款、不会错误完成、不会泄漏数据；
  - 全局唯一性：一个任务只有一个可见最终结论；
  - 全局所有权：谁能改变核心事实；
  - 外部一致性：查询、事件、终态不能互相矛盾。

  它不写“Controller”“Redis”“NodeTaskHandle”。

  ## 2. 总体架构与决策

  语义确定后，才决定如何承担它。

  总体设计回答：

  - 核心事实分别由谁拥有、谁可以写；
  - 同步/异步边界、进程边界、持久化边界；
  - 生命周期和关键提交点；
  - 模块之间的输入、输出与失败责任；
  - 重要方案选择及被拒绝的替代方案。

  这层会产生架构不变量。例如：

  - Task 状态只能由运行 Pod 的生命周期协调者写；
  - 外部取消入口只能投递命令，不能直接写 Task；
  - SSE 是投影，不反向恢复业务状态。

  它们仍不属于某个具体模块，而是模块必须共同遵守的架构规则。

  ## 3. 模块责任契约

  只有总体架构把责任分配清楚后，才创建模块设计。模块不重复描述整项能力，只回答：

  > 在上游假设成立时，我必须保证什么；我把什么保证交给下游；我明确不负责什么。

  模块不变量只能有两种来源：

  1. 从系统/架构不变量向下细化；
  2. 模块内部为了让其局部保证成立而新增的必要安全规则。

  例如 T02 的正确位置是：

  - 系统规则：取消受理后不得推进新业务动作；
  - 架构规则：Task 顶层状态由 Controller 裁决；
  - T02 局部规则：NodeRun 持久化终态前，Node Handle 不得报告已停止。

  最后一条才是 T02 自己新增的不变量。它不能重新定义“什么时候取消成功”，那是系统语义和 T05 的责任。

  模块契约应包含：

  - 输入假设；
  - 局部可观察保证；
  - 局部不变量；
  - 接口与交接约定；
  - 非责任；
  - 留给集成验证的行为。

  ## 4. 实现规格与开发任务

  实现规格是把模块契约映射到文件、类型、算法、调用顺序和异常处理。

  开发任务不应再创造新的行为语义。它只应是：

  - 一个可评审的实现切片；
  - 对应哪些模块契约；
  - 修改哪些代码位置；
  - 需要哪些局部测试；
  - 哪些契约尚未覆盖。

  这意味着 T01、T02 不是“需求的子文档”，而是“模块责任的实施切片”。

  复杂模块可以有多个任务，但这些任务共同引用同一份模块契约，不能每个任务都重新发明一套不变量。

  ## 5. 分层验证

  验证也按范围分层，不能混为一谈：

   层级             要证明的结论
  ━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   单元/模块验证    模块局部契约成立
  ───────────────  ──────────────────────────────────
   接口契约验证     上下游交接没有语义偏差
  ───────────────  ──────────────────────────────────
   集成验证         多个模块组合后维持架构与能力规则
  ───────────────  ──────────────────────────────────
   能力验收         用户可观察结果满足最初语义契约

  因此“所有 T01–T06 都通过”不自动等于“取消能力完成”；还必须有能力级集成验收来证明原始系统语义。

  ## 6. 证据与状态管理

  所有文档都使用同一组状态，但不同层级的含义不同：

  draft
  → approved
  → implementation-ready
  → locally-verified
  → integration-verified
  → capability-accepted

  最重要的纪律是：

  - 模块通过只能标记 locally-verified；
  - 只有能力级验收才能标记 capability-accepted；
  - 代码不能自行成为新语义的权威来源；
  - 若实现发现规格无法兑现，回到拥有该决策的层级修订，而不是在测试里偷偷放宽。

  我会把这套方法总结为：

  语义定义“什么是真”
  架构定义“谁负责让它为真”
  模块定义“我局部怎样保持它为真”
  实现定义“代码怎样做到”
  验证定义“凭什么相信它为真”




能力语义
  → 总体架构与责任分配
  → 拆开发任务
  → 每个任务先写任务交付契约
  -> 开发任务详细设计
  → 编码与局部验收
  → 集成验收能力


![[Pasted image 20260903142529.png]]