

方法一：中心扩展法
- 有奇数个元素时，left和right是同一个元素
- 有偶数个元素时，left和right是两个元素
- 维护一个最长的回文结果，每次扩展后进行更新
复杂度分析：长度是n的字符串，有n个奇数中心，有n-1个偶数中心，总中心数是2n-1，每次扩展最多扩展n/2词，所以复杂度是$O(n^2)$，空间复杂度为$O(1)$

```

def longestPalindrome(s: str) -> str:
	n = len(s)
	if n < 2:
		return s
	res = ""
	for i in range(len(s)):
		strOdd = expand(s, i, i)
		strEven = expand(s, i, i+1)
		if len(strOdd) > len(res):
			res = strOdd
		if len(strEven) > len(res):
			res = strEven
	return res


def expand(s: str, left: int, right: int) -> str:
	n = len(s)
	if left < 0 or right >= n:
		return ""
	while left > 0 and right < n and s[left] == s[right]:
		left -= 1
		right += 1
	# 这里想错了，最后已经多走了一步了，结果范围应该是[left+1:right]
	return s[left+1:right]
```



方法二：动态规划法
通过计算s[i:j+1]是否是回文串来遍历所有的子串，计算的方式通过动态规划，如果判断是子串那么就可以进行最长长度的更新。
- 状态定义：$dp[i][[j]$ 表示$s[i:j+1]$的字符串是否是回文串
- 状态转移
	- 如果$s[i]$和$s[j]$相等，那么$dp[i][j] = dp[i+1][j-1]$
	- 如果$s[i]$和$s[j]$不相等，那么$dp[i][j] = false$
- 如果$dp[i][[j]$ 为true的时候，更新最大长度
复杂度：时间复杂度$O(n^2)$，空间复杂度$O(n^2)$

```
def longestPalindrome(s: str) -> str:
	n = len(s)
	res = ""
	dp = [[False] * n for _ in range(n)]
	# dp[i][j] 表示s[i:j+1]是不是回文
	# 如果s[i] == s[j]: dp[i][j] = dp[i+1][j-1]
	# i要从大算到小，j要从小算到大
	
	# initialize，长度为1的元素可以看成回文，如果要初始化就要初始化长度为1和长度为2，然后更新res，这里就不背这个模板了，直接背下面这个版本
	# for i in range(n):
	# 	dp[i][i] = True
		
	# 这道题我写的时候犯了一个错误，状态转移的条件是 j - i + 1 >= 3，也就是要三个字符以上
	# 如果只有一个或者两个的时候，dp[i][j] = s[i] == s[j]
	for i in range(n-1, -1, -1):
		for j in range(i, n): 
			if s[i] == s[j]:
				if j - i >= 2:
					dp[i][j] = s[i] == s[j]
				else:
					dp[i][j] = dp[i+1][j-1]
				if j - i + 1 > len(res):
					res = s[i:j+1]
	return res
	
```