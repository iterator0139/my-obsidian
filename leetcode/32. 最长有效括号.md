给你一个只包含 `'('` 和 `')'` 的字符串，找出最长有效（格式正确且连续）括号 子串 的长度。

左右括号匹配，即每个左括号都有对应的右括号将其闭合的字符串是格式正确的，比如 `"(()())"`。

**示例 1：**

**输入：**s = "(()"
**输出：**2
**解释：**最长有效括号子串是 "()"

**示例 2：**

**输入：**s = ")()())"
**输出：**4
**解释：**最长有效括号子串是 "()()"

**示例 3：**

**输入：**s = ""
**输出：**0

**提示：**

- `0 <= s.length <= 3 * 104`
- `s[i]` 为 `'('` 或 `')'`

栈里面维护的是当前阶段的合法栈，栈底是合法串的前一个位置

遇到左括号入栈，遇到右括号出栈，如果右括号出栈后，栈为空了，就可以结算一次了。
结算完成后，再把这个位置压入栈

这里有个错误，结算时每次匹配成功的时候去做，匹配失败的时候只需要更新栈顶就好