给定一个大小为 `n` 的数组 `nums` ，返回其中的多数元素。多数元素是指在数组中出现次数 **大于** `⌊ n/2 ⌋` 的元素。

你可以假设数组是非空的，并且给定的数组总是存在多数元素。

**示例 1：**

**输入：**nums = [3,2,3]
**输出：**3

**示例 2：**

**输入：**nums = [2,2,1,1,1,2,2]
**输出：**2

**提示：**

- `n == nums.length`
- `1 <= n <= 5 * 104`
- `-109 <= nums[i] <= 109`
- 输入保证数组中一定有一个多数元素。

**进阶：**尝试设计时间复杂度为 O(n)、空间复杂度为 O(1) 的算法解决此问题。

最暴力的想法就是搞一个map统计出现次数，一但某个数字出现次数超过了一半就可以返回了

如果不能用额外的空间，但是又需要知道每个数字的频率，可以

现在看来这个方向是不行的，需要把问题进行转化，不统计数字的频率了，通过一个insight：如果把除众数外的所有数和众数一一消解，最后留下来的那个数字就是答案，维护一个candidate，遇到相同的数字就++，遇到不同的就--

