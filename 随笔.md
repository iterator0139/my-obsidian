VN: 
正排codis
redis-cli -h codis.shopeefood-ads-adsdataserver-vn.ap-vn-2-private-a.vtbd.live.cache.shopee.io -p 8156 -a "MnspRajDAeIO"

  

连接Billing Codis
redis-cli -h codis.shopeefood-ads-adsbillingsvr-vn-vn-live.ap-vn-2-general-a.vtbd.live.cache.shopee.io -p 8388 -a "kRCN1MU0VdTw"

food_ads_update_ads_store_status_1261156_live_vn

food_ads_update_ads_store_status_1211344_live_vn

food_ads_update_ads_store_status_1209927_live_vn


我现在有个每天重复的任务，收到local给的store id，我需要把这些store的欠款状态标签扭转成false

~~1 拼出对应的key~~
~~2 通过本地的smcsh脚本登录live环境的pod~~
~~3 连接billing redis~~
~~4 对所有的key get key，确认是true，再set成false~~
~~5 连接正排 redis~~
~~6 对所有的key get key，确认是true，再set成false~~

---

## ✅ 已自动化！

使用 [[scripts/gen_reset_commands.sh]] 生成一条**可靠的 heredoc 命令**，粘贴到 pod 终端自动执行：

```bash
# 方式1：直接传 store ID（多个用空格隔开）
./scripts/gen_reset_commands.sh 1261156 1211344 1209927

# 方式2：从文件读取（每行一个 ID）
./scripts/gen_reset_commands.sh store_ids.txt
```

### 工作流

```
本地执行脚本 → 生成命令（cat heredoc 创建脚本 + 执行）
         ↓
终端 smcsh → 输入密钥 → 进入 pod
         ↓
复制粘贴生成的整段命令 → 自动完成所有操作
```

### 为什么这样更稳？

之前的方案是每行命令独立粘贴，多行 shell（函数定义、if 判断）容易在终端里被截断或乱掉。

现在的方案是用 **一条 `cat` heredoc** 在 pod 里创建一个完整的脚本文件：
```
cat > /tmp/reset_debt.sh << 'PASTEHERE'
... 全部逻辑（每行一条命令，格式规整）...
PASTEHERE
chmod +x /tmp/reset_debt.sh
bash /tmp/reset_debt.sh
```

粘贴 → heredoc 把内容原样写入文件 → 执行文件 → 完成。**只有一次粘贴，不存在中断问题。**

### 生成的命令自动完成

1. ✅ 生成所有 key
2. ✅ 连接 billing Redis → 对每个 key GET 确认 → 是 true 则 SET false
3. ✅ 连接正排 Codis → 对每个 key GET 确认 → 是 true 则 SET false
4. ✅ 不是 true 的 key 自动跳过（防止重复操作覆盖）
