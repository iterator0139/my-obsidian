# 试用期目标与架构落地映射

> 状态：目标映射与验收补充，待评审  
> 适用范围：下半年 L3 级 Agent 工作流建设，聚焦主 Agent 平台开发与迭代  
> 设计输入：[`01-core-task-runtime-blueprint.md`](./01-core-task-runtime-blueprint.md)、[`11-responsibility-decoupling-design-package-v2/11-responsibility-decoupling-design.md`](./11-responsibility-decoupling-design-package-v2/11-responsibility-decoupling-design.md)、[`13-hitl-decoupled-interaction-service-design.md`](./13-hitl-decoupled-interaction-service-design.md)

> 本文不新增或替代架构决策，只将试用期目标映射到已有架构设计、阶段和验收标准。与既有文档冲突时，以当前有效的 `01`、`11-v2` 和 `13` 设计结论为准；`04-validation-and-rollout.md` 仅作为历史指标定义参考。

## 0. 目标摘要

围绕下半年搭建 L3 级 Agent 工作流，试用期重点完成三项工作：

1. **主 Agent 架构重构**：围绕任务运行时、职责边界和状态所有权，补齐面向可靠编排的基础能力。
2. **主 Agent 编排策略优化**：围绕主 Agent 的核心职责，形成“计划—执行—验证—重规划”的流程闭环，并在关键节点支持人工审核和恢复执行。
3. **评估体系建设的配合支撑与消费**：参与评估指标、用例和评估链路建设，消费评估结果识别编排问题，并将结果反哺主 Agent 策略迭代。

三个目标的关系是：

> **架构重构提供运行时基础 → 评估体系建设与编排策略优化并行推进 → 消费评估结果，反哺编排策略迭代。**

其中，评估体系建设不是本岗位独立负责的完整平台项目；本岗位重点是结合主 Agent 运行机制提供评估支撑，并将评估结果用于架构和编排策略优化。编排策略优化与评估建设可以并行，但必须在形成初步评估能力后建立稳定的“评估—分析—策略调整—回归验证”闭环。 

## 1. 范围与 L3 工作流定义

### 1.1 工作范围

本文面向主 Agent 平台开发岗位的试用期目标，不将“完成全部平台重构”作为试用期承诺。试用期优先交付可验证的核心切片和一个 L3 工作流样板，并为下半年持续演进建立指标和验收基础。

### 1.2 L3 的最小定义

本文将 L3 工作流定义为：主 Agent 能够根据任务目标编排一个包含多个执行步骤的工作流，并在预先定义的关键节点暂停，由人对过程或结果进行审核；审核结论能够驱动继续执行、补充信息、重新规划或终止流程。

| L3 特征    | 架构落点                                                 | 试用期验收关注点                                     |
| -------- | ---------------------------------------------------- | -------------------------------------------- |
| 关键节点人工审核 | `InteractionService`、`HITLCoordinator` 及其 Agno/远程适配器 | 能够打开 Interaction、记录审核结论、幂等恢复或驳回流程            |
| 结果验证     | `OutcomeVerifier`、`PlanExecuteVerifyPipeline`        | 能够输出 `PASS`、`REPLAN_REQUIRED`、`FAIL`，并驱动后续动作 |
| 可靠执行     | `TaskRun`、`Attempt`、任务状态机和运行时不变量                     | 依赖、取消、恢复、重复派发和迟到事件有确定语义                      |

`PlanVerifier` 负责计划结构、依赖和输入绑定校验，`OutcomeVerifier` 负责执行结果是否满足任务目标，二者不能混为一谈。

## 2. 三项目标与架构映射

| 试用期目标 | 主要架构切片 | 试用期核心交付 | 阶段依赖 |
| --- | --- | --- | --- |
| 主 Agent 架构重构 | `TaskRun`/`Attempt`、`PlatformTask`/`TaskLifecycle`/`TaskPipeline`、职责解耦 | 现状与目标架构、核心模块重构、状态与异常可观测性 | 优先完成 Phase 0/1 安全基础 |
| 主 Agent 编排策略优化 | `PlanService`、`PlanVerifier`、`PlanExecutor`、`PlanExecuteVerifyPipeline`、`OutcomeVerifier`、`InteractionService` | 一个可运行的 L3 工作流样板、审核恢复和结果验证闭环 | 依赖任务运行时和状态基础；与评估支撑并行，对应 Phase 2/3 |
| 评估体系建设的配合支撑与消费 | 验收不变量、决策门、Planner/Runtime/Worker/HITL/兼容性指标 | 配合评估用例和指标建设；消费评估结果，形成策略迭代输入 | 架构运行时基础就绪后，与编排策略优化并行；初步评估结果形成后反哺策略优化 |

## 3. 目标一：主 Agent 架构重构

> 本目标的具体交付以模块拆分、职责迁移和调用链收敛为主，不停留在原则或边界定义层面。执行校验不单独列项，纳入规划层与执行层解耦事项中。

### 3.1 目标

围绕主 Agent 核心链路中规划、执行、状态、上下文、事件和人工介入职责交叉的问题，完成关键模块拆分和调用关系收敛，降低核心链路的修改成本，提升复杂任务编排的稳定性和可演进性。

### 3.2 关键结果

1. **拆分 Plan 与 Execute 模块**

   **概括描述：** 将任务规划和任务执行从同一条混合链路中拆分出来，建立清晰的计划产出、执行请求和执行结果边界。

   **具体落点：**

   - 从现有主流程中拆出 Plan 模块，负责任务理解、步骤拆解、依赖关系和后续动作建议；
   - 从现有主流程中拆出 Execute 模块，负责执行请求接收、执行条件校验、远程 Agent 派发、结果接收和异常处理；
   - 明确 Plan 输出到 Execute 输入的结构化转换关系；
   - 将依赖校验、重复派发拦截和执行条件判断放入 Execute 边界内；
   - 禁止 Plan 模块直接触发远程执行副作用；
   - 统一 direct 和 loop 两类调用路径进入 Execute 模块的方式。

   **完成信号：** Plan 和 Execute 可以分别进行单元测试；Plan 变更不需要修改远程执行实现；Execute 可以拒绝不满足依赖或状态条件的执行请求；同一执行请求具备明确的执行身份。

2. **拆分并落地主流程策略 Pipeline 组装机制**

   **概括描述：** 将主 Agent 的流程控制从具体执行逻辑中抽离，形成可组合的主流程策略，支持不同任务按策略组装执行链路。

   **具体落点：**

   - 梳理现有主流程中的计划、执行、结果处理、人工审核和结束判断节点；
   - 将流程节点从 `OrchestrationWorkflow` 的具体实现中拆出，形成明确的策略步骤；
   - 建立 Pipeline 的组装入口，支持按任务类型或执行模式组合流程节点；
   - 首期至少支持现有主流程和包含人工审核的 L3 流程两种组装方式；
   - 保证 Pipeline 只负责流程编排，不直接维护远程 Agent、HITL 或展示事件的底层状态；
   - 为后续增加结果验证、重规划和其他策略保留扩展点，但不在本阶段过度建设通用框架。

   **完成信号：** 主流程不再通过大段条件分支混合表达不同策略；新增一种流程策略主要通过组装已有步骤完成，不需要复制整条 Workflow；不同策略可以独立测试和对比。

3. **拆分平台任务状态机层**

   **概括描述：** 将任务和步骤状态从 Workflow、Plan、Execute 逻辑中抽离，形成独立的任务状态管理层。

   **具体落点：**

   - 从现有 Workflow 和执行逻辑中识别任务、步骤和单次执行的状态迁移；
   - 抽取任务状态、步骤状态、执行状态、暂停状态、取消状态和未知状态的管理逻辑；
   - 为每次执行建立唯一的 `attempt_id` 或等价执行身份；
   - 统一处理状态迁移、重复执行、迟到结果和旧结果覆盖问题；
   - 明确状态机与 Plan、Execute、HITL、事件投影之间的接口；
   - 优先完成当前运行链路可使用的最小状态机，不在本阶段承诺跨 Pod 自动接管。

   **完成信号：** 任务状态不再由多个模块分别维护；任务恢复可以读取统一状态；旧执行结果不能覆盖当前状态；取消、暂停和恢复能够通过状态机表达。

4. **拆分上下文构建层**

   **概括描述：** 将上下文准备和结果传递从 Plan、Execute 及 Workflow 主流程中抽离，减少不同职责之间的上下文混杂。

   **具体落点：**

   - 抽取原始执行结果、结构化观察结果、规划上下文和人工审核材料的构建逻辑；
   - 建立统一的上下文构建入口，分别为 Planner、Executor 和 HITL 提供所需上下文；
   - 将大结果、artifact、敏感信息和摘要处理从具体业务步骤中抽离；
   - 明确上下文引用、摘要和原始结果之间的关系；
   - 记录上下文来源、大小和基本 token 成本，为后续评估和策略优化提供数据；
   - 避免上下文构建层直接推进任务状态或触发远程执行。

   **完成信号：** Planner、Executor 和 HITL 使用明确的上下文入口；结果摘要策略可以独立调整；上下文变化不会直接改变任务状态和执行控制流。

5. **拆分事件消息与展示投影层**

   **概括描述：** 将任务事实、内部事件消息和前端展示事件分离，降低事件消息与核心任务控制逻辑之间的耦合。

   **具体落点：**

   - 从主流程中抽取事件发布、事件过滤、事件持久化和 SSE 投影逻辑；
   - 区分任务状态事实、审计事件和前端展示事件；
   - 为事件补充任务、步骤、attempt 和序列关联信息；
   - 处理事件重复、乱序、迟到和断线重放；
   - 移除通过展示事件反向推进任务状态的隐式逻辑；
   - 保证事件投影失败不会影响任务事实和执行状态。

   **完成信号：** SSE 或展示事件重复不会导致重复执行；事件乱序不会覆盖任务状态；任务恢复不再以展示事件作为唯一事实来源；前端重放与 Runtime 状态相互独立。

6. **隔离 HITL 子系统**

   **概括描述：** 将人工审核从具体的主流程和远程执行实现中隔离出来，使 HITL 成为可复用的暂停、回答、恢复和取消能力。

   **具体落点：**

   - 从主 Agent 和远程 Agent 执行链路中抽取 HITL 请求创建、状态维护、回答处理和恢复触发逻辑；
   - 统一 Agno HITL 和远程数字员工 HITL 的暂停、回答、恢复、超时和取消接口；
   - 将 HITL 状态与平台任务状态机建立关联，但不让 HITL 直接修改 Plan 或绕过 Execute；
   - 保留现有 HITL 协议差异，通过适配器隔离底层实现；
   - 统一记录审核结果、审核历史和后续动作；
   - 本阶段优先完成现有两类 HITL 路径的隔离，不提前建设覆盖未知编排形态的通用 Interaction 平台。

   **完成信号：** 主流程只依赖统一的 HITL 接口；Agno 和远程 HITL 的协议差异不再扩散到 Plan/Execute；审核通过、驳回、超时和取消都能驱动明确的任务动作。

### 3.3 事项之间的依赖关系

```text
Plan / Execute 拆分
        ↓
平台任务状态机层拆分
        ↓
Pipeline 组装机制落地
        ├── 上下文构建层拆分（可并行）
        ├── 事件消息与展示投影层拆分（可并行）
        └── HITL 子系统隔离（依赖状态机和 Execute 边界）
```

其中，Plan/Execute 拆分是本目标的首要交付；任务状态机和 Pipeline 组装机制共同形成主流程和运行时基础；上下文、事件投影和 HITL 隔离可以在接口明确后分批推进。Pipeline 组装机制只建设满足当前真实流程需要的最小能力，不以抽象出完整通用编排框架为目标。结合试用期范围，最终以完成 Plan/Execute 拆分、任务状态机拆分，以及至少一个配套边界模块拆分作为核心验收范围。 

### 3.4 交付物与验收

- Plan、Execute、任务状态机、Pipeline、Context、Event Projection 和 HITL 的模块职责及调用关系说明；
- 完成 Plan 与 Execute 的模块拆分及至少一条真实调用链迁移；
- 完成最小平台任务状态机，并接入执行、暂停、恢复和取消流程；
- 完成至少两种主流程策略的 Pipeline 组装；
- 完成上下文、事件投影和 HITL 中至少两个模块的边界拆分；
- 补充依赖、重复派发、状态迁移、事件重放、HITL 恢复和远程取消测试。

验收重点不是模块数量，而是职责迁移是否真实发生：Plan 不直接触发外部副作用，Execute 不承担计划生成，任务状态有明确所有者，事件投影不反向控制 Runtime，HITL 不绕过任务状态机。

## 4. 目标二：主 Agent 编排策略优化

### 4.1 目标

围绕主 Agent 的核心职责，优化任务拆解、依赖调度、子 Agent 协同、结果验证和失败重规划策略，形成一个包含人工审核的 L3 工作流闭环。编排策略优化与评估体系建设并行推进，并持续消费评估结果进行策略调整和回归验证。

本目标对齐 `11-v2` 的 `PlanExecuteVerifyPipeline`，以及 `13` 提出的 `InteractionService` 和可恢复执行的 `Suspended` 终态。目标一的运行时安全基础未完成前，不扩大真实调度范围。

### 4.2 关键结果

1. 支持至少 3 类典型编排场景：顺序依赖、并行执行、失败阻断或条件分支；前后置依赖的执行顺序可通过自动化测试验证。
2. 在至少 1 个真实或准真实业务场景中落地 L3 工作流样板，完整覆盖：任务拆解、子 Agent 执行、结果汇总、关键节点审核、审核后恢复和最终输出。
3. 结果验证支持 `PASS`、`REPLAN_REQUIRED`、`FAIL` 三类结论，并分别对应继续执行、重新规划或失败处理。
4. 关键审核节点覆盖率达到 **100%**；支持审核通过、驳回、补充信息、超时和取消等基本路径；HITL 恢复成功率目标为 **≥95%**。
5. 至少完成 1 个远程 Agent 场景的端到端验证，覆盖认证、trace/run ID 关联、取消和结果映射。
6. 对重复派发、迟到事件、未知 attempt、超时和取消失败形成明确日志或事件，关键异常可定位率目标为 **≥90%**。

### 4.3 交付物与验收

- 一个可运行、可演示的 L3 工作流样板；
- 编排策略、状态流转和审核节点说明；
- `OutcomeVerifier` 及失败重规划的实现或适配方案；
- 端到端测试记录和异常场景验证报告。

验收流程为：

> 主 Agent 识别任务 → 生成计划 → 按依赖调度子 Agent → 汇总并验证结果 → 关键节点人工审核 → 继续执行、重规划或终止 → 输出最终结果。

## 5. 目标三：评估体系建设的配合支撑与消费

### 5.1 目标

配合建设面向主 Agent 的可重复评估体系，结合主 Agent 运行机制和编排策略提供评估场景、指标口径、数据采集和问题分析支撑；同时消费评估结果，识别计划、调度、执行、HITL 或结果验证中的问题，并将结论反哺架构和编排策略迭代。

本目标不以独立负责完整评估平台为要求，而以“能支撑主 Agent 评估、能消费评估结果、能推动策略改进”为核心。指标分类沿用 `04-validation-and-rollout.md` 的历史定义，当前验收优先以 `01` 中的验收不变量和决策门为准。

### 5.2 评估维度与关键结果

| 评估维度 | 重点指标 | 试用期关键结果 |
| --- | --- | --- |
| Planner 质量 | 可提交计划率、无效依赖率、重规划有效率、最终结果人工评分 | 建立可复现任务集，完成至少 2 轮版本对比 |
| Runtime 正确性 | 非法状态迁移、依赖违规派发、重复 dispatch、迟到事件拒绝 | 核心不变量纳入自动化回归；依赖违规派发和非法状态迁移为 0 |
| Worker 可靠性 | 提交成功率、observe 延迟、timeout、cancel 确认率、未知 attempt 比率 | 建立按 `task_run_id` 关联的采集和问题分类 |
| HITL 体验 | Interaction 等待时间、重复回答率、resume 成功率、过期率 | 覆盖审核通过、驳回、超时和取消路径 |
| 兼容性 | SSE schema 错误、前端重放失败、旧 checkpoint 恢复成功率 | 核心 API/SSE 和 continue/cancel 路径完成回归 |
| 成本与容量 | token、工具调用、Worker 时间、结果大小、排队时长 | 先建立可采集基线，不在无数据时承诺吞吐或延迟 |

### 5.3 交付物与验收

1. 配合建设不少于 **30 条**评估用例，覆盖简单任务、多 Agent 顺序/并行、失败重试、结果不通过、人工审核、远程 Agent、取消和恢复等场景；其中主 Agent 负责的场景和指标有明确责任边界。
2. 支撑编排正确性自动化覆盖率达到 **≥80%**，参与至少 **2 轮**主 Agent 版本回归。
3. 能够消费评估结果，按 Planner 质量、Runtime 正确性、Worker 可靠性、HITL 或兼容性对问题进行分类，并形成至少 **2 个**可执行的策略或架构优化项。
4. 将策略优化结果重新纳入评估集进行回归，形成至少 **1 个“评估—分析—策略调整—回归验证”闭环**。
5. 配合输出评估指标定义、版本对比报告和 L3 工作流上线准入建议；不将完整评估平台建设作为本岗位独立交付范围。

## 6. Landing 计划

以下计划以 12 周试用期为示例；具体日期可根据入职时间调整。目标一先提供运行时基础；目标二编排策略优化与目标三评估体系建设在基础具备后并行推进，形成初步评估结果后，再将评估结论反哺策略迭代。

| 阶段         | 时间        | 重点工作                                                             | 阶段产出与退出条件                                  |
| ---------- | --------- | ---------------------------------------------------------------- | ------------------------------------------ |
| 现状理解与基线    | 第 1—2 周   | 熟悉主 Agent、Workflow、子 Agent、远程调用和 HITL 链路；选择 2—3 个业务场景；梳理状态流和问题清单 | 现状架构图、问题清单、目标架构初稿、评估场景初版和基线指标              |
| 架构安全阀与核心修复 | 第 3—5 周   | 对齐 Phase 0/1；修复依赖派发问题；补齐状态、异常、认证、取消和 trace 关联；建立核心测试 | 依赖违规派发为 0；核心回归通过率目标 ≥95%；主要异常可定位 |
| 编排策略与评估支撑并行 | 第 6—9 周 | 一边对齐 Phase 2/3 实现计划—执行—验证—重规划和 L3 审核样板，一边配合建设评估用例、指标采集和问题分类 | 1 个端到端 L3 工作流；形成初步评估结果；输出至少 1 个策略优化输入 |
| 评估反哺与阶段总结 | 第 10—12 周 | 消费评估结果，调整编排策略并回归验证；完善评估集和版本对比；输出准入建议和后续规划 | ≥30 条评估用例；评估自动化覆盖率目标 ≥80%；至少 1 个评估反哺策略闭环；形成阶段总结 |

阶段推进遵循以下发布纪律：先验证状态、依赖、取消和恢复等语义正确性，再扩大并发和真实流量；影子规划不得产生重复外部副作用；未知 attempt 进入对账，不通过盲目重派提升表面成功率。

## 7. 重点学习项目

### 7.1 Agent 编排框架与主 Agent 运行机制

学习 Agent、Workflow、Tool Call、事件流、流式后台执行、取消恢复、checkpoint/continue 和远程 Agent 协议。学习成果必须绑定业务交付：输出运行机制说明，能够独立定位编排链路中的状态、调度和恢复问题，并转化为至少一个架构改造或问题修复。

### 7.2 Agent 评估与 L3 质量保障

学习计划质量与执行质量的区分、规则评估、结构化评估、LLM Judge、人工抽检、HITL 节点设计、评估集构建和灰度发布。学习成果不是独立建设完整评估平台，而是能够理解评估结果、定位主 Agent 问题，并将评估结论转化为策略调整和回归验证。

## 8. 风险、依赖与决策门

- 远程 Agent 的幂等、查询和取消语义未完全确认时，只在可控场景中进行真实调度；未知状态进入 reconcile，不盲目重试。
- `InteractionService` 仍属于设计草案时，试用期先通过现有 HITL 适配器验证流程语义，避免形成第二套事实来源。
- 新旧执行路径并行时，必须明确单个 `TaskRun` 的执行 owner，不能在运行中无条件切换控制面。
- 指标基线不足时，优先建设采集和分类能力，不将未经验证的吞吐、延迟或成功率写成平台承诺。
- 关键决策门包括远程幂等和恢复语义、任务与成本基线、HITL 快照恢复、API/SSE 兼容性；未关闭决策门不因试用期排期而放宽架构红线。

## 9. 与既有文档的关系

- `01-core-task-runtime-blueprint.md` 是长期目标蓝图，提供任务运行时分阶段落地、验收不变量和决策门。
- `11-responsibility-decoupling-design-package-v2/` 是职责解耦的当前设计入口，提供 Plan、Execution、HITL、事件和 Agent 能力注册的职责划分。
- `13-hitl-decoupled-interaction-service-design.md` 是 HITL 解耦方向的补充草案，本文将其作为 L3 人工审核的目标落点。
- `04-validation-and-rollout.md` 为历史验证与发布草案，本文仅复用其指标分类，不改变其“已被 01 收敛替代”的状态。
- 本文只定义试用期目标、交付物和验收映射；如需调整既有不变量、架构职责或阶段顺序，应通过正式评审或 ADR 处理。
